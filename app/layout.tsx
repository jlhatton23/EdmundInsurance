import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700', '800'],
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Insurance Solution Center — Compare Rates & Save',
  description:
    'Compare auto, home, life, business, health, and pet insurance quotes from top carriers side by side. Save thousands on your premium.',
  keywords: 'insurance comparison, auto insurance quotes, home insurance, compare insurance rates',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
